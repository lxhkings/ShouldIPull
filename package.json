{
  "name": "shouldipull",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "build": "node build.js"
  },
  "dependencies": {
    "@vercel/kv": "^3.0.0"
  }
}
